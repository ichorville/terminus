/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import {FormSubmitCompleteEvent} from './form-submit-complete-event';

describe('FormSubmitCompleteEvent', () => {
  it('should create an instance', () => {
    //expect(new FormSubmitCompleteEvent()).toBeTruthy();
  });
});
