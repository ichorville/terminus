/* tslint:disable:no-unused-variable */

import { async, inject } from '@angular/core/testing';
import {DeleteEvent} from './delete-event';

describe('DeleteEvent', () => {
  it('should create an instance', () => {
    // expect(new DeleteEvent()).toBeTruthy();
  });
});
