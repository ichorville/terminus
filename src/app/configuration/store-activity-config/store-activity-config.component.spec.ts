/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StoreActivityConfigComponent } from './store-activity-config.component';

describe('Component: StoreActivityConfig', () => {
  it('should create an instance', () => {
    let component = new StoreActivityConfigComponent();
    expect(component).toBeTruthy();
  });
});
