/* tslint:disable:no-unused-variable */
import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import { OutletConfigNavTabComponent } from './outlet-config-nav-tab.component';

describe('OutletConfigNavTabComponent', () => {
  // let component: OutletConfigNavTabComponent;
  // let fixture: ComponentFixture<OutletConfigNavTabComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ OutletConfigNavTabComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(OutletConfigNavTabComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
